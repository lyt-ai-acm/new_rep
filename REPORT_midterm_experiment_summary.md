# 中文社交媒体情感分析：纠错增强分类实验总结（中期）

## 1. 研究目标
本项目旨在验证“**文本纠错（n-best）+ 情感分类**”是否能提升中文社交媒体文本（Weibo）二分类效果。  
核心问题：在噪声文本场景中，候选纠错与融合策略（E1/E2/E3）是否优于基础模型（Base）。

---

## 2. 实验流程与已完成工作

### 2.1 数据与划分
- 数据集：`Weibo_senti_100k`
- 已完成：训练/验证/测试切分（当前汇报基于 dev 集）
- 样本字段：
  - 原始集：`review`, `label`
  - n-best 集：`orig`, `cand_1...cand_10`, `w_1...w_10`

### 2.2 候选纠错生成
- 分词：Jieba
- 语言模型：KenLM（3-gram, prune 20k，降配可运行版）
- 已生成：
  - `models/lm_jieba_3gram_20k.arpa`
  - `models/lm_jieba_3gram_20k.klm`
  - `outputs/norm/dev_top10_jieba.csv`（含 top10 候选及对应权重）

### 2.3 分类模型
- Backbone：`chinese-roberta-wwm-ext`
- 任务：二分类（正/负）
- 训练配置（E0）：
  - epoch=1, batch=16, lr=2e-5, max_len=128, seed=42
- 已完成本地权重部署与离线加载。

---

## 3. 对比设置（Methods）
- **Base_orig**：直接对原句 `review` 进行分类（不做纠错融合）
- **E1_top1**：仅使用 `cand_1`
- **E2_top10_avg**：10个候选预测概率等权平均
- **E3_top10_weighted**：10个候选预测概率按 `w_i` 加权平均

---

## 4. 实验结果（Dev）

| Setting | Accuracy | F1 | Precision | Recall |
|---|---:|---:|---:|---:|
| **Base_orig** | **0.9795** | **0.9792** | **0.9935** | 0.9653 |
| E1_top1 | 0.9778 | 0.9775 | 0.9909 | 0.9645 |
| E3_top10_weighted | 0.9776 | 0.9773 | 0.9899 | 0.9650 |
| E2_top10_avg | 0.9749 | 0.9747 | 0.9839 | **0.9657** |

---

## 5. 结果分析
1. **基础模型最优**：Base 在 Accuracy/F1 上均优于 E1/E2/E3。  
2. **E2 下降最明显**：等权平均将低质量候选噪声一并引入。  
3. **E3 优于 E2 但仍弱于 Base**：权重融合有效缓解噪声，但候选质量仍是瓶颈。  
4. 当前结论：在现有 LM 与候选生成设置下，纠错增强尚未带来净收益。

---

## 6. 创新与后续优化方案（提升准确率）

> 目标：使纠错增强方案在 test 集上达到 **F1 ≥ Base**，并争取超过 Base。

### 6.1 融合策略创新（优先级最高）
#### A. 温度加权融合（Temperature-weighted E3）
对 LM 权重做幂次重标定：
\[
\tilde{w}_i=\frac{w_i^\alpha}{\sum_j w_j^\alpha}
\]
- 待调：`alpha ∈ {0.5, 1.0, 1.5, 2.0}`
- 预期：`alpha>1` 强化高质量候选，抑制尾部噪声。

#### B. 置信门控回退（Fallback Gating）
当候选不可靠时回退原句预测：
- 条件1：`w1 < τ`（如 0.35）
- 条件2：`w1 - w2 < δ`（如 0.08）
满足任一条件则使用 Base 结果。
- 预期：减少“纠错误伤”，提升稳健性。

#### C. Top-K 自适应
比较 `K ∈ {3,5,10}`，避免固定 K=10 引入噪声。
- 预期：K=5 可能优于 K=10。

---

### 6.2 候选生成优化（LM侧）
1. **提高 LM 容量**：3-gram 20k → 3-gram 50k（资源允许可尝试 5-gram 50k）  
2. **保守纠错约束**：
   - 每句最多替换 1~2 字符
   - 不替换 `@用户`、URL、emoji、数字串  
3. **候选过滤**：
   - 删除与原句编辑距离过大候选
   - 删除低频/低置信候选

---

### 6.3 分类器训练优化（Model侧）
1. 调参网格：
   - `lr`: 1e-5 / 2e-5 / 3e-5
   - `epochs`: 2 / 3
   - `batch_size`: 8 / 16
   - `max_len`: 128 / 192
   - `warmup_ratio`: 0.06 / 0.1
2. 训练策略：
   - 启用 `fp16`（若稳定）
   - early stopping（基于 dev F1）
   - 多 seed 汇报（42/3407/2025）

---

### 6.4 新增可解释评估（论文加分项）
- 按样本长度分桶（短文本/长文本）统计收益
- 按噪声类型统计（拼写/口语缩写/表情符）
- 抽取失败案例做错误分析（为何 E2/E3 被误导）

---

## 7. 下一阶段实验计划（可执行）
1. **复现实验到 test 集**：Base/E1/E2/E3 全部跑齐。  
2. 实现并评估三项创新：
   - E3 + 温度加权（alpha）
   - E3 + 门控回退（tau, delta）
   - Top-K 自适应（K=3/5/10）  
3. 输出最终主表：
   - Dev 用于调参，Test 用于最终结论
   - 报告均值±方差（多 seed）

---

## 8. 当前阶段结论
本阶段完成了从数据处理、候选生成到分类评估的全链路搭建。  
实验显示：**当前纠错融合尚未超过基础模型**，但通过“温度加权 + 置信门控 + 候选约束”的组合优化，预期可将纠错从“噪声源”转为“增益源”，是下一阶段的关键突破点。